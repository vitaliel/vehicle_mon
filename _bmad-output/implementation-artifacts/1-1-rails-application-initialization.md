# Story 1.1: Rails Application Initialization

Status: done

## Story

As a developer,
I want the Rails 8.1 application initialized with PostgreSQL, Propshaft, RSpec, Devise, and Bootstrap layout,
So that all future stories have a correct, consistent foundation to build on.

## Acceptance Criteria

1. **Given** the repository is cloned, **When** `bundle install && rails db:create` is run, **Then** the application boots without errors and the PostgreSQL database is created.

2. **Given** the app is running, **When** any page is requested, **Then** Bootstrap 5.3.8 styles are applied (loaded via bundled assets — `gem 'bootstrap'` + `application.scss`) and Bootstrap Icons are available.

3. **Given** the app is running, **When** any page is requested, **Then** the layout renders the `shared/_flash_messages` partial (not inline `<p>` tags).

4. **Given** the app is running, **When** any page is requested, **Then** `app/views/shared/_vehicle_card.html.erb` exists (may be empty/stubbed).

5. **Given** the Gemfile, **When** reviewed, **Then** it includes `devise (~> 5.0)`, `rspec-rails`, `factory_bot_rails`, `shoulda-matchers`; `--skip-test` (no minitest) and `--skip-jbuilder` were applied.

6. **Given** the repo, **When** a push is made to any branch, **Then** GitHub Actions CI runs RSpec and reports pass/fail.

## Current State Analysis (Pre-Dev Intelligence)

**Already Done (do NOT redo):**
- ✅ `rails new vehicle_mon --database=postgresql --asset-pipeline=propshaft --skip-test --skip-jbuilder` — skeleton exists
- ✅ Gemfile has `gem 'devise', '~> 5.0'`, `gem 'rspec-rails'`, `gem 'factory_bot_rails'`, `gem 'shoulda-matchers'`
- ✅ `.github/workflows/ci.yml` exists (runs brakeman, bundler-audit, rubocop)
- ✅ Rails 8.1.3 with PostgreSQL confirmed
- ✅ **Bootstrap 5.3.8 bundled** via `gem 'bootstrap'` + `app/assets/stylesheets/application.scss` (`@import "bootstrap"`) — NO CDN needed for CSS
- ✅ **Bootstrap JS bundled** via `vendor/javascript/bootstrap.js` + `@popperjs--core.js` pinned in `config/importmap.rb` — NO CDN needed for JS
- ✅ `app/views/layouts/application.html.erb` already has `stylesheet_link_tag :app` and `javascript_importmap_tags`

**Still Needs Implementation:**
- ❌ **Bootstrap Icons** not yet set up — add CDN `<link>` for Bootstrap Icons in layout `<head>` (icons font only, no JS needed)
- ❌ `app/views/shared/` directory does NOT exist
- ❌ `app/views/shared/_flash_messages.html.erb` does NOT exist
- ❌ `app/views/shared/_vehicle_card.html.erb` does NOT exist
- ❌ Layout uses inline `<p class="notice">/<p class="alert">` instead of flash partial
- ❌ `ci.yml` does NOT have an RSpec job — only security scan and lint jobs

## Tasks / Subtasks

- [x] Task 1: Update application layout with Bootstrap Icons and flash partial (AC: #2, #3)
  - [x] Add Bootstrap Icons CDN `<link>` to `<head>` in `application.html.erb` (CSS only — no JS needed for icons)
  - [x] Replace inline `<p class="notice">` and `<p class="alert">` with `<%= render 'shared/flash_messages' %>`
  - [x] **Do NOT add Bootstrap CSS or JS CDN tags** — already bundled via gem + importmap

- [x] Task 2: Create shared view partials (AC: #3, #4)
  - [x] Create `app/views/shared/` directory
  - [x] Create `app/views/shared/_flash_messages.html.erb` — renders Bootstrap alert dismissible divs for `notice` and `alert` flash keys
  - [x] Create `app/views/shared/_vehicle_card.html.erb` — stub partial (empty or minimal HTML comment)

- [x] Task 3: Add RSpec job to GitHub Actions CI (AC: #6)
  - [x] Add a `test` job to `.github/workflows/ci.yml` that sets up Ruby, installs gems, creates the test database, and runs `bundle exec rspec`
  - [x] Job should use a PostgreSQL service container

- [x] Task 4: Verify RSpec configuration is complete
  - [x] Confirm `spec/rails_helper.rb` and `spec/spec_helper.rb` exist (generated by `rails generate rspec:install`)
  - [x] If missing, run `rails generate rspec:install`
  - [x] Ensure `spec/support/` directory exists for future support files

### Review Findings

- [x] [Review][Patch] CI trigger does not run on push to any branch [`.github/workflows/ci.yml`:2]

## Dev Notes

### Bootstrap Integration — Bundled Assets (NOT CDN)

**Bootstrap CSS** is loaded via the `bootstrap` gem + asset pipeline:
- `gem 'bootstrap'` is in `Gemfile` ✅
- `app/assets/stylesheets/application.scss` has `@import "bootstrap"` ✅
- The layout already has `<%= stylesheet_link_tag :app %>` ✅
- **Do NOT add a Bootstrap CSS CDN `<link>` tag** — it's already bundled.

**Bootstrap JS** is loaded via importmap with vendored files:
- `vendor/javascript/bootstrap.js` and `@popperjs--core.js` exist ✅
- `config/importmap.rb` has `pin "bootstrap" # @5.3.8` ✅
- The layout already has `<%= javascript_importmap_tags %>` ✅
- **Do NOT add a Bootstrap JS CDN `<script>` tag** — it's already bundled.

**Bootstrap Icons** — add only the icons font CDN `<link>` (CSS only, no JS):
```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.css">
```
Place this in `<head>` of `application.html.erb`, after the existing stylesheet tag.

### Flash Messages Partial
The `_flash_messages` partial must use Bootstrap's dismissible alert component. Pattern:
```erb
<% flash.each do |type, message| %>
  <% css_class = type == 'notice' ? 'alert-success' : 'alert-danger' %>
  <div class="alert <%= css_class %> alert-dismissible fade show" role="alert">
    <%= message %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>
```
Flash keys used in this app are exclusively `notice` (success) and `alert` (error) — no others.

### GitHub Actions RSpec Job
Add to `.github/workflows/ci.yml` a new `test` job:
```yaml
test:
  runs-on: ubuntu-latest
  services:
    postgres:
      image: postgres:16
      env:
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: postgres
        POSTGRES_DB: vehicle_mon_test
      ports: ['5432:5432']
      options: >-
        --health-cmd pg_isready
        --health-interval 10s
        --health-timeout 5s
        --health-retries 5
  env:
    RAILS_ENV: test
    DATABASE_URL: postgres://postgres:postgres@localhost:5432/vehicle_mon_test
  steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        bundler-cache: true
    - name: Set up database
      run: bin/rails db:create db:schema:load
    - name: Run tests
      run: bundle exec rspec
```

### Project Structure Notes
- Do NOT touch Devise installation/configuration — already done (commits show `rails devise and rspec`)
- Do NOT re-run `rails new` — the skeleton exists
- Do NOT modify `config/database.yml` unless the CI job fails to connect
- The `app/assets/stylesheets/application.css` may already exist — leave it; Bootstrap is loaded via CDN, not asset pipeline
- `spec/` directory should already exist from Devise/RSpec installation; verify before running `rspec:install`
- Layout file path: `app/views/layouts/application.html.erb` — confirmed existing

### Architecture Compliance Checklist
- [ ] `flash[:notice]` / `flash[:alert]` only — no other flash keys (enforced by partial design)
- [ ] Bootstrap 5.3.8 exact version via CDN
- [ ] `shared/_flash_messages` partial included in layout
- [ ] `shared/_vehicle_card` partial exists (can be empty stub)
- [ ] CI runs RSpec on push

### References
- Bootstrap 5.3.8 CDN: https://getbootstrap.com/docs/5.3/getting-started/download/#cdn-via-jsdelivr
- Bootstrap Icons CDN: https://icons.getbootstrap.com/#install
- Architecture: `_bmad-output/planning-artifacts/architecture.md` — "Frontend Architecture", "Project Structure"
- Epics: `_bmad-output/planning-artifacts/epics.md` — Story 1.1 (lines 154–181), ARC1, ARC2, ARC6, ARC7

## Dev Agent Record

### Agent Model Used

claude-sonnet-4.6

### Debug Log References

### Completion Notes List

- Story implemented 2026-04-27.
- Layout: Bootstrap Icons CDN `<link>` added; inline flash `<p>` tags replaced with `render 'shared/flash_messages'`.
- `app/views/shared/_flash_messages.html.erb` created with Bootstrap dismissible alert pattern (notice → alert-success, alert → alert-danger).
- `app/views/shared/_vehicle_card.html.erb` was already present as empty stub — left as-is.
- `.github/workflows/ci.yml` `test` job completed with PostgreSQL 16 service container and `bundle exec rspec`.
- `spec/rails_helper.rb` and `spec/spec_helper.rb` already existed; `rspec:install` run to confirm identical.
- `spec/support/` created with `factory_bot.rb`, `shoulda_matchers.rb`, `devise.rb` support files.
- Support autoloader uncommented in `rails_helper.rb`.
- All 3 existing specs pass (0 failures).

### File List

**Modified:**
- `app/views/layouts/application.html.erb` — Bootstrap Icons CDN link + flash partial render
- `.github/workflows/ci.yml` — completed `test` job with PostgreSQL + RSpec
- `spec/rails_helper.rb` — uncommented support file autoloader

**Created:**
- `app/views/shared/_flash_messages.html.erb`
- `spec/support/factory_bot.rb`
- `spec/support/shoulda_matchers.rb`
- `spec/support/devise.rb`

**Pre-existing (verified):**
- `app/views/shared/_vehicle_card.html.erb` (empty stub)
- `spec/rails_helper.rb`, `spec/spec_helper.rb`
